# Neon — pooled connection string from the Neon dashboard (Settings → Connection details)
DATABASE_URL=

# Vercel Blob — create a store in the Vercel dashboard → Storage → Blob → .env snippet
BLOB_READ_WRITE_TOKEN=
